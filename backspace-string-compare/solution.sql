
                push(First.charAt(i));
            }
            else{
                pop(First.charAt(i));
            }
        }

        for(int i=0; i<t.length(); i++){
            if(s.charAt(i)!=#){